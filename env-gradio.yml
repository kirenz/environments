name: gradio
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip<=23.0
  - pip:
      - pandas==2.0.3
      - openpyxl
      - ipykernel
      - notebook
      - jupyterlab
      - openai
      - python-dotenv
      - redlines
      - panel
      - jupyter_bokeh
      - tiktoken
      - utils
      - gradio
      - "transformers[torch]"
